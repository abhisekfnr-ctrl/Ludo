ROYAL LUDO - ALL FIXES APPLIED

1. Room join now changes status to PLAYING when required player count is reached.
2. Game state is initialized immediately for 2/4 player rooms.
3. Multiplayer game-state writes are restricted to the current player's turn to prevent stale clients from overwriting each other.
4. Game state values are normalized server-side to valid Ludo ranges.
5. Client publish requests are queued to reduce overlapping state-write races during pawn animation.
6. Admin dashboard crash fixed: removed reference to missing #dice element.
7. Admin Force Turn UI fixed.
8. Admin Clear Dice action verified/fixed.
9. Room-specific admin dice remains the active dice-control mechanism.
10. Tested: Node syntax, browser JS syntax extraction, room create/join, PLAYING transition, game state initialization, turn protection, admin login/data, set dice, clear dice, force turn, pause/resume.

Note: The default admin password remains change-me-123 for compatibility. Set ADMIN_PASSWORD before public deployment.
