Royal Ludo v29 / v2.2.0 - Board, Dice, Pawn & Navigation Fixes

FIXES:
1. Goti entry corrected: on rolling 6, a goti enters on its own START/arrow cell.
2. Goti movement path corrected for all four players; no extra 2-cell offset.
3. Capture/safe-cell calculation corrected to use the same real board path.
4. Local pawn animation is protected from 350ms online polling, so the moving goti no longer jumps back/flickers.
5. Movement animation is faster and smoother (140ms per cell).
6. Dice controls remain enabled only for the active player and are protected from accidental double-rolls.
7. Added LOBBY button: cleanly leaves the current online room and returns to lobby.
8. Added REFRESH GAME button for a safe in-game refresh.
9. Added server /api/rooms/leave endpoint; leaving a room removes the player and reindexes remaining players.
10. Added no-store cache headers to API responses to prevent stale game/lobby data.
11. Existing login/register and admin-dice functionality retained.
